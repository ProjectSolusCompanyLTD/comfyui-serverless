name: Build and Push ComfyUI Worker

on:
  push:
    branches: [ main ]   # runs whenever you push to main
  workflow_dispatch:      # allows you to trigger manually

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false  # avoid pulling any LFS blobs

      - name: Free disk space on runner
        run: |
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache
          sudo rm -rf "$AGENT_TOOLSDIRECTORY" || true
          docker system prune -af || true
          rm -rf ~/.cache || true
          echo "Disk after cleanup:" && df -h

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          # optionally cache layers in GitHub to reduce rebuild work
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/comfyui-custom:v1
            ${{ secrets.DOCKERHUB_USERNAME }}/comfyui-custom:latest
